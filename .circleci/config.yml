version: 2.1
orbs:
  node: circleci/node@3.0.0
jobs:
  build:
    executor:
      name: node/default
    steps:
      - checkout
      - node/install-packages
      - run:
          command: npm install
      - run:
          command: npm run test
workflows:
  build:
    jobs:
      - build-and-test
